import { Component } from '@angular/core';
import { RouterModule } from '@angular/router';

@Component({
  selector: 'app-score-setting',
  standalone: true,
  imports: [RouterModule],
  template: '<router-outlet />'
})
export class ScoreSettingComponent {
}
